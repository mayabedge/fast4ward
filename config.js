// Admin (+ button) settings. Fill these in after creating the GitHub repo. See setup/README.md.
const ADMIN_CONFIG = {
  owner: "",      // GitHub username that owns the repo
  repo: "",       // repository name
  branch: "main",
  lockedToken: null, // paste the JSON from setup/lock-token.html here
};
